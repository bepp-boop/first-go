name: ADES100Composite
on: 
  workflow_dispatch:
    inputs:
      name: 
        default: "World"

runs:
  using: composite
  steps:
    - name: Apply env var fix
      run: |
        echo "Hello ${{ inputs.name }}"
      shell: bash
