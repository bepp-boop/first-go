name: ADES101Composite

on: 
  workflow_dispatch:
    inputs:
      name: 
        default: "World"

runs:
  using: composite
  steps:
    - name: Example step
      uses: actions/github-script@v6
      with:
        script: console.log('Hello ${{ inputs.name }}')
